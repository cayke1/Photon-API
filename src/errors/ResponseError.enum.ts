export enum ErrorCodes {
  userNotFound = 'AUTH_NOT_FOUND',
  invalidPassword = 'AUTH__INVALID_PASSWORD',
  tokenNotSent = 'AUTH__TOKEN_NOT_SENT',
  expiredToken = 'AUTH__EXPIRED_TOKEN',
  invalidToken = 'AUTH__INVALID_TOKEN',
}
